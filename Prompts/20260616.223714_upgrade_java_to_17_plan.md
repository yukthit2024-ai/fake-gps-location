# User Prompt
upgrade java to 17

---

# Implementation Plan - Upgrade Java to 17

We will upgrade the Java source and target compatibility versions to Java 17.

## Proposed Changes

### Build Configuration

#### [MODIFY] [build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Mock_GPS_Location_APK_GITHUB/app/build.gradle)

We will modify the `compileOptions` block to use `JavaVersion.VERSION_17` instead of `JavaVersion.VERSION_1_8`.

```groovy
    compileOptions {
        // java.time (Instant, DateTimeFormatter) requires desugaring below API 26 OR minSdk 26+
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
```

## Verification Plan

### Automated Tests
We will build the project using gradle wrapper to ensure it compiles correctly with Java 17:
- `./gradlew assembleDebug`
