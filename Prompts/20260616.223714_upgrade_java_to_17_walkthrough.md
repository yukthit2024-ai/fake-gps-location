# User Prompt
upgrade java to 17

---

# Walkthrough - Upgrade Java to 17

We have upgraded the Java version to 17 in the project settings.

## Changes Made

### Build Configuration

#### [app/build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Mock_GPS_Location_APK_GITHUB/app/build.gradle)

Upgraded the compiler source and target compatibility:
```diff
     compileOptions {
         // java.time (Instant, DateTimeFormatter) requires desugaring below API 26 OR minSdk 26+
-        sourceCompatibility JavaVersion.VERSION_1_8
-        targetCompatibility JavaVersion.VERSION_1_8
+        sourceCompatibility JavaVersion.VERSION_17
+        targetCompatibility JavaVersion.VERSION_17
     }
```

## Verification

We verified the build configurations. Since the execution environment command was denied, the project compiles should be verified locally by running:
```bash
./gradlew assembleDebug
```
